# macOS 磁盘空间分析工具

一个强大的命令行工具，用于分析 macOS 文件系统的磁盘空间使用情况。

## 功能特点

- 🔍 递归扫描目录，统计空间占用
- 📊 显示最大的文件和目录
- 📁 按文件类型（扩展名）分类统计
- 🌳 可视化目录树结构
- 📈 文件大小分布图
- 🎨 彩色终端输出
- 📄 支持导出 HTML/JSON 报告

## 安装

### 一键安装（推荐）

```bash
git clone https://github.com/openingax/disk-analyzer.git
cd disk-analyzer
./install.sh
source ~/.zshrc
```

安装脚本会自动：
- 创建 Python 虚拟环境
- 安装所需依赖
- 添加 `disk-analyzer` 命令到 shell

### 手动安装

```bash
# 进入项目目录
cd FileAnalyze

# 设置 Python 版本（使用 pyenv，可选）
pyenv local 3.11.9

# 创建虚拟环境
python3 -m venv .venv

# 激活虚拟环境
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 卸载

```bash
./uninstall.sh
source ~/.zshrc
```

## 使用方法

### 基本用法

```bash
# 分析当前目录
python main.py

# 分析指定目录
python main.py ~/Downloads

# 分析整个磁盘（需要完全磁盘访问权限）
sudo python main.py /
```

### 常用选项

```bash
# 限制扫描深度
python main.py ~ --depth 3

# 显示前 30 个最大项
python main.py . --top 30

# 只统计大于 10MB 的文件
python main.py . --min-size 10MB

# 排除特定目录
python main.py . --exclude node_modules,.git,__pycache__

# 生成 HTML 报告
python main.py . --output report.html

# 生成 JSON 报告
python main.py . --json report.json
```

### 完整参数列表

| 参数 | 简写 | 说明 | 默认值 |
|------|------|------|--------|
| `path` | - | 要分析的目录路径 | `.` |
| `--depth` | `-d` | 最大扫描深度 | 无限制 |
| `--top` | `-n` | 显示前 N 个最大项 | 15 |
| `--min-size` | `-m` | 最小文件大小 | 0 |
| `--exclude` | `-e` | 排除的目录（逗号分隔） | - |
| `--output` | `-o` | 输出 HTML 报告 | - |
| `--json` | `-j` | 输出 JSON 报告 | - |
| `--tree-depth` | - | 目录树显示深度 | 2 |
| `--no-color` | - | 禁用彩色输出 | - |
| `--follow-symlinks` | - | 跟随符号链接 | 否 |
| `--show-errors` | - | 显示所有扫描错误 | - |

## 完全磁盘访问权限

要扫描系统的某些受保护目录（如 `/Library`、`/System`），需要授予终端完全磁盘访问权限：

1. 打开 **系统设置** > **隐私与安全性** > **完全磁盘访问权限**
2. 点击 **+** 添加 **终端.app** (或你使用的终端应用)
3. 重启终端

## 输出示例

```
===========================================================
  磁盘空间分析报告
===========================================================

  📁 扫描路径: /Users/username/Downloads
  💾 总大小:   15.3 GB
  📄 文件数:   1,234
  📂 目录数:   89

📂 最大的目录
------------------------------------------------------------
   1.   15.3 GB [████████████████████] 100.0%  /Users/username/Downloads
   2.    8.2 GB [██████████░░░░░░░░░░]  53.6%  .../Videos
   3.    4.1 GB [█████░░░░░░░░░░░░░░░]  26.8%  .../Archives

📄 最大的文件
------------------------------------------------------------
   1.    2.1 GB  .../large-video.mp4
   2.    1.5 GB  .../backup.zip
   3.  800.0 MB  .../disk-image.dmg
```

## 许可证

MIT License
